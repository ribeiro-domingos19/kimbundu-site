// ===========================================
// routes/comments.js (LIMPO)
// ===========================================

const express = require('express');
const router = express.Router();

// Todas as rotas de comentários foram movidas para lessons.js.

module.exports = { router };

